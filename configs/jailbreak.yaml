# Configuration for jailbreak adversarial image generation using AdvBench

# Model settings
cache_dir: "/home/adewinmb/orcd/scratch"
model_name: "qwen3inst"  # Options: "llava", "qwen3inst", "qwen3think"

# Training settings
num_epochs: 30
learning_rate: 0.01
epsilon: 1.0  # 255/255
steps_per_epoch: 100
checkpoint_dir: null # ex. 'experiments/20251105_120632'

# Dataset settings
dataset_type: "jailbreak"  # Uses walledai/AdvBench dataset
train_split: 0.7
val_split: 0.2
test_split: 0.1

# Image initialization
initial_image: "black"  # Options: "black", "noise", "expedia"

# Target string (not used for jailbreak dataset - labels come from AdvBench)
target_str: null

# Evaluation settings
num_examples: 10

# Output settings
log_dir: null  # If null, will be auto-generated with timestamp

