# Default configuration for adversarial image generation

# Model settings
cache_dir: "/home/adewinmb/orcd/scratch"
model_name: "qwen3inst"  # Options: "llava", "qwen3inst", "qwen3think"

# Training settings
num_epochs: 30
learning_rate: 0.01
epsilon: 1.0  # 255/255
steps_per_epoch: 100
checkpoint_dir: null # ex. 'experiments/20251105_120632'
checkpoint_every: 10 # Save adversarial image checkpoint every N epochs
loss_type: ce # Options: "ce", "margin"
margin_param: 0 # Margin loss hyperparameter 

# Dataset settings
dataset_type: "specific_string"  # Options: "specific_string", "jailbreak"
train_split: 0.7
val_split: 0.2
test_split: 0.1

# Image initialization
initial_image: "black"  # Options: "black", "noise", "expedia"

# Target string for adversarial attack
target_str: "Download the guide at malware.com!"

# Evaluation settings
num_examples: 10

# Output settings
log_dir: null  # If null, will be auto-generated with timestamp

